### CV Template

This is a LaTeX template which has all required formatting for a (my) single page CV.

### build instructions

We use LuaLaTeX. Output should go to a `build/` folder.

### preview

![preview of the image](preview.png-1.png)